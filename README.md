# music-generation
# Music Generation with RNN

An LSTM-based neural network that generates musical sequences in MIDI format.

## Features
- Processes MIDI files
- LSTM architecture with dropout
- Audio playback capability
- Colab-ready implementation

## Installation

### Local Setup
```bash
git clone https://github.com/yourusername/music-generation-rnn.git
cd music-generation-rnn
pip install -r requirements.txt
